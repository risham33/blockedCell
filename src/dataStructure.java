/*
 * THIS CLASS WOULD BE THE OBJECT CLASS OF THE WHOLE ENTIRE PROGRAM, WHICH WOULD CONTAIN IF THE 
 * FOLLOWING BLOCK IS VISITED OR NOT
 * IF THE OBJECT IS BLOCKED THEN THE VALUE OF THAT OBJECT WOULD BE CHANGED TO 'O', IF IT IS NOT BLOCEKD
 * THEN IT WOULD BE 'X' 
 * If it is the start point, then it would be changed to S and then if it is an end point then it would 
 * changed to E 
 */
public class dataStructure {
	boolean visited = false;
	boolean blocked = false;
	char value = 'x';
}
